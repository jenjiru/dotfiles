alias sudo='sudo '

alias ..='cd ..'

alias yay='paru'
alias yeet='paru -Rcs'
alias clean='paru -Rsn $(paru -Qdtq) 2>/dev/null || echo Nothing to clean'
alias install='paru -Sy $@ --needed'

alias bios-reboot='systemctl reboot --firmware-setup'

alias ns='nsxiv -a'

alias v='nvim'
alias vi='nvim'
alias vim='nvim'
alias nano='nvim'

alias background='feh --bg-scale'
alias a-iso='/opt/arch-iso.sh > /dev/null  2>&1 & disown'
alias wlan='/opt/wlan-connect'
alias kvm-network='sudo virsh net-start default'
alias torrent='aria2c --seed-time=0.01 -u 0 -d $HOME/downloads' 

alias v-nova='v $HOME/documents/github/nova/nova.sh'
alias v-main='v $HOME/documents/github/nova/main-pc.sh'

alias ls='eza -l'
alias la='eza -la'
alias lsg='eza'
alias lag='eza -a'

#alias cp='cp -gR'
#alias mv='mv -g'
#alias cp=cpg -g
#alias cpr=cpg -gr
#alias mv=mvg -g
#alias mvr=mvg -gr

alias vpn='sudo surfshark-vpn'
alias vpn-help='sudo surfshark-vpn help'
alias vpn-connect='sudo surfshark-vpn attack'
alias vpn-multi='sudo surfshark-vpn multi'
alias vpn-off='sudo surfshark-vpn down'
alias vpn-status='sudo surfshark-vpn status'
alias vpn-logout='sudo surfshark-vpn forget'
alias vpn-version='sudo surfshark-vpn version'

alias wget=wget --hsts-file="$XDG_DATA_HOME/wget-hsts"
